# .github/workflows/bootstrap.yml
name: Bootstrap Workflow
on:
  push:
    branches:
      - main  # or master if that's your default branch

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: echo "✅ First push detected, triggering Welcome Issue workflow"
