name: Bootstrap Workflow
on:
  push:
    branches: [ main ]  # change to master if needed

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - run: echo "First push detected. Handing off to Create Welcome Issue."
